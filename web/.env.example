# Backend base URL (the FastAPI service in ../api).
METRON_API_URL=http://localhost:8000

# Placeholder tenant until auth lands (PH4). The backend requires an X-Tenant-Id
# header; set this to a tenant UUID you've created via the API to view its data.
METRON_DEV_TENANT_ID=
